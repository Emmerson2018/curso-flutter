name: notes_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  drift: ^2.33.0
  drift_flutter: ^0.3.0       # integração nativa Flutter
  sqlite3_flutter_libs: ^0.6.0+eol # binários SQLite para Android/iOS
  flutter_riverpod: ^3.3.1
  path_provider: ^2.1.5
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  drift_dev: ^2.33.0

flutter:

  uses-material-design: true
